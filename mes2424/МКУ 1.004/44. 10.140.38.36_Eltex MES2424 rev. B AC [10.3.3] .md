Прошивка: 10.3.3  

1. Заходим с вышестоящего коммутатора из той же подсети.  
1.1 Подключаемся telnet-ом к Eltex-у.  

2. Смотрим текущий шлюз: show ip route  
3. Удаляем маршрут no ip route 0.0.0.0 0.0.0.0 [ip-адрес шлюза]  
4. Создаём новый gateway:  

ip route 0.0.0.0 0.0.0.0 10.245.151.62  

5.  

interface vlan 99  
description MGMT  
ip address 10.245.151.xx 255.255.255.192  
no ipv6 enable  
no shutdown  
exit  

6. Теперь заходим через PUTTY по новому IP.  
И настраиваем SNTP.  

sntp  
 set sntp client enabled  
 set sntp client addressing-mode unicast  
 set sntp unicast-poll-interval 16384  
 set sntp unicast-server ipv4 10.6.37.21 priority 1  
 set sntp unicast-server ipv4 10.6.36.38 priority 2  
exit  
clock time source ntp  
clock utc-offset +07:00  

7. Теперь настроим РАДИУС.  

aaa authentication default radius local // Чтобы работали и локальный админ и Радиус-учётка.  
radius-server host 10.6.39.1 auth-port 2030 acct-port 2050 key "сюда вбиваем ключ" primary  
radius-server host 10.6.39.162 auth-port 2030 acct-port 2050 key "сюда ещё раз вбиваем тот же самый ключ"  

8. Теперь открываем ещё одно окно PUTTY, не закрывая старое и пробуем зайти под РАДИУС-УЧЁТКОЙ.  
Как только зайдём - вбиваем вот такую команду:  

[LLDP]  

[Это глобально]  
set lldp enable  
set lldp version v2  

[Это на порту]  
  lldp tlv-select basic-tlv port-descr  
  lldp tlv-select basic-tlv sys-name  
  lldp tlv-select basic-tlv sys-descr  
  lldp tlv-select basic-tlv sys-capab  
  lldp tlv-select basic-tlv mgmt-addr ipv4 10.245.151.14  
  lldp tlv-select dot1tlv port-vlan-id  
  lldp tlv-select dot1tlv mgmt-vid  

_____________________________________________________________________  

// Это из KB.  

#LLDP  

(config-if)#lldp tlv-select basic-tlv mgmt-addr all  
(config-if)#lldp tlv-select basic-tlv port-descr  
(config-if)#lldp tlv-select basic-tlv sys-capab  
(config-if)#lldp tlv-select basic-tlv sys-descr  
(config-if)#lldp tlv-select basic-tlv sys-name  
(config-if)#lldp tlv-select dot1tlv mgmt-vid  
(config-if))#lldp tlv-select dot1tlv port-vlan-id  
(config-if))#lldp transmit  
(config-if))#lldp receive  

_____________________________________________________________________  

[SYSLOG]  

[Отправка логов на сервер]  

logging-server facility local0 severity emergencies ipv4 10.245.2.210  
logging-server facility local0 severity alerts ipv4 10.245.2.210  
logging-server facility local0 severity critical ipv4 10.245.2.210  
logging-server facility local0 severity errors ipv4 10.245.2.210  
logging-server facility local0 severity warnings ipv4 10.245.2.210  
logging-server facility local0 severity notification ipv4 10.245.2.210  
logging-server facility local0 severity informational ipv4 10.245.2.210  
 
[Настраиваем локальное логирование]  
[Это 100% работает на прошивке 10.3.4 и 10.3.3]  

syslog localstorage  
syslog file 1 log1  
logging severity informational  
logging facility local0  
 
logging-file file 1 <-- Включаем запись в файл (просто указываем слот или имя)  

[SNMP]  
snmp user ertkro  
snmp group ertkro user ertkro security-model v2c  
snmp access ertkro v2c read iso notify iso  
snmp community index 1 name [сюда пишем read-only ключ] security ertkro  
  
snmp user ertkrw  
snmp group ertkrw user ertkrw security-model v2c  
snmp access ertkrw v2c read iso write iso notify iso  
snmp community index 2 name [сюда пишем read-write ключ] security ertkrw  
  
snmp view iso 1 included  
  
snmp targetparams ertktraps user ertkro security-model v2 message-processing v2  
snmp targetaddr EQM param ertktraps 10.245.2.210 taglist ertktag  
snmp notify ertkro tag ertktag type Trap  
  
mac-address-table notification change  
mac-address-table notification change interval 0  

copy running-config startup config  
